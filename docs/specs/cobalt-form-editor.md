# Source-defined form editor

Framerail edits the optional backend `Found.form` payload using ordered source-defined fields. Schema/value parsing and preservation remain backend responsibilities; see [data forms](cobalt-data-form-schema.md).

## What it must do

- [x] Preserve unchanged scalar types, including numeric text values and nulls; defaults display without generating updates.
- [x] Submit only changed editable known fields; omit static and unknown values from `form_updates`, draft form values, and content payloads. Select codes retain scalar types; text/wiki edits are strings.
- [x] Construct mutually exclusive `form_updates` and `wikitext` wire payloads, including empty update maps.
- [x] Render ordered static/text/select/wiki fields with visible labels, source hints, defaults and supported numeric dimensions. A select with 2–4 choices uses labeled radio buttons; larger sets remain dropdowns. Radio choices retain numeric/string/boolean/null codes, including distinct numeric `1` and string `"1"` values. Missing or unrecognized stored values are not silently replaced by the first choice. Static fields retain `properties.label` and render a nonempty scalar label before their escaped readonly value; empty or missing labels add nothing and never fall back to the field name. Readonly values preserve exact whitespace. The archive examples are `Summary`, `Additional Content Warnings`, and `Digest?`. Lowercase `hint` is a placeholder on text/wiki controls, not a separate help node; uppercase `Hint` is unsupported. Text height 1 or absent uses an input; height 2+ uses a textarea with valid width/height as columns/rows. A nonempty scalar `after` appears as escaped help adjacent to the editable control and is its accessible description, never its placeholder or default. Empty `after` adds no help or description.
- [x] Use form controls whenever a form exists; retain the raw editor only for nonform/template pages.
- [x] Create pages in a data-form category through the category form (Wikidot's NewPage → form flow). `page_create_permission` returns the category form to users who may create the page; the missing-page editor shows `DataFormFields` (defaults prefilled, title from `/title/<name>`) and save, preview and draft send `form_updates`. The stored source is Wikidot's record: every non-static field in schema order as posted text (submitted, else default, else empty).
- [x] Preserve existing title, alternate title, tags, comments, revision and authentication handling through the edit action.
- [x] Preview raw or structured draft content through the SvelteKit action without publishing, creating a revision, or changing stored page/source/form data. The trusted request identity and access check precede submitted form validation; anonymous and spoofed requests are denied.
- [x] Save one site/page-target draft shared by authorized editors for either current-page edit or creation. This target/ownership model is a documented source-based inference, not verified current-server behavior.
- [x] Store draft `title` and `wikitext` exactly. Derive complete typed form values, including unknown keys, from stored source on read. Preserve unchanged restored source bytes; merge complete values when fields are edited.
- [x] On a saved draft, offer Edit Original or Edit Draft before normal save; original publishing discards the draft. Cancel offers leave or delete.
- [x] Authorize draft access against an existing origin page; creation-only permission must not expose a draft whose private source page was moved or deleted. Preserve inaccessible orphan rows without automatic relocation.
- [x] Provide table, code, URL, page-link, image, and equation-reference wizards for raw editors. Code wraps the captured range; the other five insert at its start without consuming selected text. Equation references insert `Eq.([[eref label]])` or bare `[[eref label]]`, not a wrapper around selected text.
- [x] Use the trusted current page for image attachment lookup, requiring both View and Edit; return active files whose latest revision is an image. A missing target returns no attachment lookup result rather than creating it.
- [x] Open image **Check** in a source-style separate `about:blank` popup, report unavailable images, reject invalid schemes without opening it, and close from the popup. It must not alter the editor source or revision.
- [x] Use a dedicated `editor_pages` page-link lookup rather than general search. At `baa78f3` the client sends the untrimmed typed query through its trusted request context; at `2e0aab1` the local backend converts every ASCII space to `-`, matches a case-insensitive literal slug prefix, sorts by slug, returns current titles of nondeleted pages, checks View before its 20-result cap, and scopes to the trusted site/user. The local cap is intentional; the published PHP source is unbounded.

## How it works

- [Form schema contract](cobalt-data-form-schema.md)
- Official Wikidot [data forms reference](https://www.wikidot.com/doc-data-forms:reference): its `label` comment says, “This is what the user sees when using the form,” supporting visible labels.
- The raw wikitext formatting toolbar uses 22×22 icons and the three-row control order measured from the authenticated Wikidot editor (`/tmp/claude/wikidot-editor-reference/toolbar-dom.json`, `toolbar-styles.json`, September 23, 2026). Its local `framerail/static/cobalt-editor/icons1.png` is the source sprite from `https://d3g0gp89917ko0.cloudfront.net/v--05014b438f4f/common--theme/base/images/editor/icons1.png` (SHA-256 `d4b09792783e799e3dfd3acc0590f59783a9875909b5baf88f43f5d384d6acd3`); heading levels 2–6 and directional clear-float controls are nested. The mounted raw editors use the implemented 36 source-backed transformations. This evidence applies to raw editors, not form wiki fields.
- Hosted disposable Writing editor evidence (`/home/osso/.local/share/cobalt-wiki/source/form-editor-disposable/writing-controls-dom.json`, `writing-form.html`, `writing-archive-schema.json`, September 24, 2026) shows Author hint as placeholder, Summary text width 80/height 3 as a textarea, and adjacent `after` text on Image, Additional Content Warnings, Arc, and Chain. The [official handbook](https://handbook.wikidot.com/en:data-forms) specifies height 1 as normal text and height 2+ as a text box. This evidence covers presentation, not watcher/checkbox semantics or server-side enforcement.
- Historical form evidence does not establish a wiki-toolbar gap: `Wikidot_Form_Field_Wiki::renderEdit()` returns an escaped plain `<textarea>`, and the retained `PageEditModule.js` initializes `WIKIDOT.Editor` only when the page is not `edit-with-form`. The local plain wiki textarea therefore matches the retained historical source. Current hosted wiki-toolbar behavior is unverified. `Form_Renderer` copies definitions and adds an editor but does not demonstrate `after` semantics; its old field dispatcher has no `static` case, so it cannot establish modern static rendering or rich formatting. The unrelated `MailFormAction` validation is not evidence for data-form save validation.
- Raw-editor keyboard behavior is bounded by hosted `WIKIDOT.editor.pretty.js` lines 807–910. Source shortcuts are `Ctrl` plus lowercase `b`, `i`, or `u`, unless `Alt` is held; the key string is case-sensitive. `Meta` and `Shift` are not encoded: Meta alone is not a shortcut, while `Ctrl+Meta` plus a lowercase shortcut key still matches. `Tab` (key code 9) falls through to generic `insertText`; at an empty caret it inserts a literal tab, while a nonempty trimmed selection is retained and the caret collapses. This contract does not claim that Tab replaces the selection.
- Enter assistance runs sequentially after the native newline: `listEnd`, `list`, `listNested`, `completeBlock`, `definitionList`, `keepIndent`, then `indentEnd`. Closing code, embed-video, math, and embed constructs preserves the caret before the closing tag; source newline-prefix quirks are retained. A modern composition guard prevents IME interference, but this adaptation is not hosted-source-verified behavior.
- Preview requests are form-encoded as required by SvelteKit actions.

### Source wizard evidence and contract

The hosted editor implementation is `/tmp/claude/wikidot-editor-reference/WIKIDOT.editor.pretty.js`; dialog fields come from the older public reference `/home/osso/Repos/wikidot/web/files--common/editor/dialogs.html`. The latter supplies field/default evidence, not current hosted-browser parity. Local implementation and proof are listed below; they do not establish live lookup or visual parity.

- Code wraps the captured selection range, or inserts a selected placeholder at an empty caret. The other five wizards—table, URL, page link, image, and equation reference—insert at the captured selection start without consuming selected text. `erefWizard` inserts a reference to a labelled equation, optionally surrounded by `Eq.(…)`; it does not wrap selected text or create an equation.
- Table defaults to 3 rows and 3 columns, with an optional first-row header. The older dialog permits only two input characters (`maxlength="2"`); integer validation and a 1–99 range are local implementation inference, not source behavior.
- Local implementation supports the source-derived table/code/URL/page-link/image/equation forms. Image accepts URI, attached-file, and Flickr syntax; its position choices map to no position, left, right, center, float-left, and float-right. It intentionally omits the older template's unused extra-CSS field and does not expose size options, which were not captured. Existing Flickr syntax normalization is not supported Flickr image integration; Flickr-specific support is explicitly out of scope below.
- Hosted editor source lines 619–653 open **Check** in a separate half-screen `about:blank` popup titled `Checking image...`; on load it sizes to the image plus 200 pixels, bounded by `screen.avail* - 100`, centers the window, and supplies unavailable-image alt text plus a close link. `6270040` implements that separate-popup flow with safe DOM construction rather than the source document's `document.write`; `6dda819` supplies the helper proof. Isolated Chromium passed 4/4 (load, unavailable 404, invalid scheme without popup, close) at `/tmp/claude/cobalt-image-check-isolated-green.log`. Authenticated full-app proof passed 1/1 at `563d9d7` (`/tmp/claude/cobalt-image-popup-browser-auth-cache.log`): attached preview plus popup load/404/invalid-scheme/close, with source and revision unchanged. The two pure geometry-bound tests plus three SSR/component tests passed 5/5 (`/tmp/claude/cobalt-async-popup-targeted-node-6dda819.log`); the geometry proof covers actual desktop resize/move bounds, not visual comparison. Scoped lint/style/format passed (`/tmp/claude/cobalt-async-popup-scoped-lint-6dda819.log`); final new-test/type gates remain pending.

  Playwright Fetch interception routes with `httpCredentials` stall the initial `about:blank` popup image request. This is a test-transport limitation, not an application defect: the isolated un-intercepted browser passes, synthetic auth reproduces the stall, and clearing the test auth adapter after protected-gateway login restores it. Browser tests therefore use passive audit and persisted readback; the origin auth cache retains gateway credentials while server authentication is untouched. No global headers, userinfo URL, or compatibility fallback is used. Retire the adapter workaround when the test transport can route the popup image request without stalling.

- Code offers empty type plus `Cpp`, `CSS`, `PHP`, `HTML`, `diff`, `Java`, misspelled source value `JavaScipt`, `Perl`, `Python`, `Ruby`, `SQL`, and `XML`.
- URL defaults to `http://`, has optional anchor text, and defaults the new-window checkbox off. Page link takes page name plus optional anchor; the hosted JavaScript begins at two characters after a 0.5-second delay and requests `PageLookupQModule` with only `s`, `module`, and `q`. The published PHP source replaces every ASCII space with `-`, performs a case-insensitive `unix_name` prefix match, and orders by `unix_name`; title matching occurs only if `title=yes`, which this wizard does not send. `baa78f3` replaces local general-search reuse with `editor_pages`, preserving raw query spaces after client validation; `2e0aab1` implements the local permission-filtered, slug-sorted, literal-prefix endpoint with current nondeleted titles and a 20-visible-result cap after View checks. Browser proof at `/tmp/claude/cobalt-editor-pages-browser.log` passed 1/1 after local build/restart: prefix and sort suggestions plus all six wizard inserts and Preview. Native default-parallel proof passed 5/5 at `024240b` with a deterministic fixture's unique site and encoded-slug marker (`/tmp/claude/cobalt-editor-pages-parallel-fixture-green.log`), independently verifying the generated artifact and removing the former shared-`Other`-text deadlock. These prove the local contract only; exact hosted-server autocomplete implementation is not confirmed beyond the published PHP/current-client contract.
- Attachment lookup authorizes the trusted site/page/user for both View and Edit, includes only active files whose latest revision is an image, and excludes attachments for a missing target. Browser proof covers an empty attachment list, a missing page with no attached-file choice, and the imported `images` page: selecting an authorized filename loads its thumbnail and rendered Preview image without changing source/revision. The separate attachment test also rejects invalid Flickr input and inserts numeric Flickr syntax without fetching Flickr.
- Equation reference scans the editor's current source for labelled math blocks, displays their source as escaped text, and offers `Eq.(number)` or bare-number output. No network lookup is needed.

### Save Draft contract and provenance

The user selected `sameasWikidot`; the contract therefore follows captured hosted-editor behavior where evidence exists and source-based inference where it does not. The public `gabrys/wikidot` snapshot 0.90 (July 2009; HEAD September 2009) has no page-draft storage or actions. Captured hosted JavaScript shows title/source saving through `synchronize`, **Edit Original**/**Edit Draft** restoration, Cancel **Leave**/**Delete**, and publishing the original discarding the draft. It does not establish current server ownership, visibility, or lifecycle behavior.

The implemented contract infers one shared draft per site/page target, gated by **View** and **Edit** for an existing page, or **Create** for a missing target. It is not a per-actor draft contract. The hosted read-only check returned `status="ok"`, `draftExists=true`, and `publishedPagePresent=false`; it confirms an unpublished draft exists but captured no source Save/Cancel/Delete action or mutation.

Local proof is not hosted-server proof. `/tmp/claude/cobalt-page-draft-browser-fifth.log` passed 1/1: a missing target saves title/source without a published page or search hit; leave/reopen and **Edit Draft** restore it; publishing after **Edit Draft** or **Edit Original** clears it; **Cancel → Delete** explicitly deletes it; and a restored structured draft, then edit, preserves numeric and unknown values. Anonymous get/save/delete RPCs and SvelteKit actions were denied. Native draft tests passed 13 at `835eaf7`; `9d20256` separately proved the delete-response RED (`null`) to GREEN (`{ deleted: true }`) and the local backend alone was deployed.

Draft rows store inline `wikitext` and `title`; `form_values` is derived on read, not a separate stored map. Restoration returns unchanged raw bytes; changed source uses a valid JSON/YAML merge. The security boundary is the origin page: creation-only access cannot recover a draft from a private page after move/deletion. Such orphan rows remain inaccessible and are not automatically relocated.

Independent bounded audit (agent 363) confirmed the browser assertions and one read-only SQL reconciliation: both fixture pages have matching persisted source hashes and no remaining draft rows. At `7445c2a`, Svelte checking reports zero errors/warnings and scoped ESLint, Stylelint, and Prettier pass (`/tmp/claude/cobalt-draft-final-main-*.log`). Independent Rust fmt/check proof is retained from agent 360. Post-SSR hydration passes (`/tmp/claude/cobalt-draft-final-hydration.log`); SSR servers now use isolated temporary caches. Pre-existing route/config/dependency deprecations remain outside these scoped results.

## Implementation inventory

- `framerail/src/lib/form-editor.ts`: scalar transport types, draft/diff model and exclusive content payload.
- `framerail/src/lib/component/DataFormFields.svelte`: source-defined controls and labels.
- `framerail/src/routes/[slug]/[...extra]/EditorPane.svelte`: selects form or raw editor, mounts the toolbar and wizard dialogs for raw create/edit, restores source selection after insert/cancel, and submits changed fields.
- `framerail/src/lib/wikitext-keyboard.ts`: source-bounded raw-editor shortcut, Tab, and Enter assistance, including the modern composition guard.
- `framerail/src/lib/wikitext-wizards.ts`: pure wizard validation, source generation, selection insertion, equation extraction, and Flickr normalization.
- `framerail/src/lib/component/WikitextWizard.svelte`: six modal wizard forms, delayed page suggestions, attachment selection, URI preview, and escaped equation-source preview.
- `framerail/src/lib/server/load/editor-lookup.ts`, `deepwell/src/endpoints/editor_lookup.rs`: trusted page-suggestion transport and current-page image attachment lookup.
- `framerail/src/lib/component/EditorPreview.svelte`, `framerail/src/lib/server/load/page-preview.ts`, `deepwell/src/endpoints/page_preview.rs`: form-encoded preview action and authorized no-write rendering.
- `framerail/src/lib/server/deepwell/views.ts`: optional backend form response type.
- `framerail/src/lib/server/load/page.ts`: forwards form data and validates exclusive edit inputs.
- `framerail/src/lib/server/deepwell/page.ts`: sends the selected content variant to Deepwell.

## Tests asserting this spec

- `framerail/tests/local/form-create.mjs`: browser 1/1 on an isolated local stack (2026-09-23): NewPage "Create Character Profile" → form editor with prefilled title and no raw source → save stores `player: ''`, `name: '…'`, `'@@'` defaults, `sex: female` in template order, and the live template shows the name. At `f6fd542`, independent scoped checks pass: Svelte reports 0 errors and 0 warnings; ESLint, Prettier, and Node syntax checks pass (`/tmp/claude/cobalt-form-create-types-{svelte-check,eslint,prettier,node-check}.log`). The Svelte log retains, without suppressing, the missing-`pnpm`, CSRF deprecation, and Sass `if()` deprecation stderr.
- Static-label browser proof passed 2/2 at `9e34591` (`/tmp/claude/cobalt-static-label-browser-9e34591.log`): a no-write Preview of Writing New Page's 28 fields and a Character-form regression, preserving original source/revisions and the missing target. Test-only typing/complexity refactor `d8964be` reran Writing with `node --test tests/local/writing-form.mjs` under its protected password-file environment; it passed 1/1 (`/tmp/claude/cobalt-writing-browser-d8964be.log`). These proofs cover nonempty escaped labels, exact static-value whitespace, and no writes; static values remain excluded from drafts and content payloads by contract. They are not runtime proof of the archive inventory (8 templates, 168 fields, all four supported types). Independent verifier 503 passed at `d8964be`: ESLint had 0 diagnostics, Prettier passed, Svelte check exited 0 with 0 errors/0 warnings, readability measured maximum cognitive complexity 7 and cyclomatic complexity 8 (`assertDefaultControl`), all functions below thresholds, and diff check passed 0 (`/tmp/claude/cobalt-writing-form-d8964be-{eslint,prettier,svelte-check,readability}.log`). Svelte check stderr still includes unrelated missing-`pnpm` config-path output and Sass dependency deprecations.
- Category-form browser acceptance passed 5/5 at `a87f367` (`/tmp/claude/cobalt-category-forms-a87f367-browser.log`) for deliberately missing Player, Chain, Arc, BGC, and Application targets. Every schema-ordered field rendered with defaults, visible typed options, visible labels, exact escaped static text, and a structured Preview; template source/revision and target absence remained unchanged before Preview, after Preview, and finally. The explicit archived-consumer markers rendered in Preview are Player `pronouns` → `PlayerBox`, Chain `summary` → `ChainBox`, Arc `arcSummary` → `ArcBox`, BGC `name` → `BGCBox`, and Application `pronouns` directly.

  | Category    | Fields | Field kinds                        | Browser result                                              |
  | ----------- | -----: | ---------------------------------- | ----------------------------------------------------------- |
  | Player      |     11 | 2 static, 6 text, 3 wiki           | defaults, labels/static text, typed options, Preview marker |
  | Chain       |      5 | 2 static, 3 text                   | defaults, labels/static text, Preview marker                |
  | Arc         |     10 | 4 static, 5 text, 1 wiki           | defaults, labels/static text, Preview marker                |
  | BGC         |     24 | 8 static, 5 select, 9 text, 2 wiki | defaults, labels/static text, typed options, Preview marker |
  | Application |     35 | 23 static, 12 wiki                 | defaults, labels/static text, Preview marker                |

  The five categories cover 85 fields. The initial run at `2da9454` passed 2/5; `/tmp/claude/cobalt-chain-preview-boundary.log` proved its test selected an image URL rather than the rendered marker, not a renderer defect. No product renderer change followed. Together with retained Character (33) and Writing (28) no-write proof, browser coverage is seven categories and 146 fields. Independent verifier 518 passed the scoped tooling at `a87f367`: ESLint and Prettier passed; Svelte check exited 0 with 0 errors and 0 warnings; readability measured maximum cognitive complexity 8 and cyclomatic complexity 10 (`/tmp/claude/cobalt-category-forms-a87f367-{eslint,prettier,svelte-check,readability}.log`). Its manual read found the cohesive 34-line `assertRadioField` helper and suggested, but did not require, extracting it; extraction is deferred under minimal scope. This is not a no-findings claim. Independent verifier 527 later ran the native Svelte project check at `9e79652`: exit 0, 0 errors, 0 warnings (`/tmp/claude/cobalt-category-forms-99bfb2f-character-form-06c13ad-svelte-check.log`). Existing `pnpm`, CSRF, and Sass stderr remains disclosed. Both fixtures also passed frontend lint/format; maximum category metrics were cognitive 9/cyclomatic 13 and Character 8/10. Initial narrow TypeScript configuration errors were environmental and are not established repository failures.

- NPC parser compatibility is approved and passed standalone compatibility 19/19 at `4f687c9` (`/tmp/claude/cobalt-npc-compatibility.log`). Its narrow parser rule accepts only a non-flow block-mapping value that starts alphanumeric, has no earlier colon, and ends in a colon, yielding `orc` / `Orc:` for the archived option; it is not broad trailing-colon support. The historical Symfony result is separate evidence for the old parser, not hosted behavior. The pre-deploy missing-page run failed at `page_create_permission` with RPC 4000 (`/tmp/claude/cobalt-npc-form-99bfb2f-schema-red.log`). Root `./deploy.sh` then exited 0 (`/tmp/claude/cobalt-npc-local-deploy-4f687c9.log`) and local unit PID 2896759 was active. On that current deployed local build, the NPC case passed: all 22 fields (7 static, 9 text, 4 select, 2 wiki), exact `Orc:` race label/default/options, Preview, reload, and no-write behavior (`99bfb2f`). This is local browser evidence, not hosted behavior; see the [data-form schema SSOT](cobalt-data-form-schema.md#historical-npc-parser-evidence).

  | Category | Fields | Field kinds                        | Browser result                                                |
  | -------- | -----: | ---------------------------------- | ------------------------------------------------------------- |
  | NPC      |     22 | 7 static, 9 text, 4 select, 2 wiki | exact `Orc:` label/default/options, Preview, reload, no write |

  The eight archived categories now have per-case browser definitions covering 168 fields: Character 33, Writing 28, Player 11, Chain 5, Arc 10, BGC 24, Application 35, and NPC 22. The combined eight-form browser attempt at `4f687c9` passed NPC, Chain, Arc, BGC, Application, and Writing but had Player login 500 and Character marker failures (`/tmp/claude/cobalt-eight-forms-4f687c9-browser.log`, 6 pass/2 fail). After `06c13ad`, the original Player and Character polling cases both still failed marker observation (`/tmp/claude/cobalt-player-character-06c13ad-browser.log`). Instrumented Player and Character runs passed while recording that the request and response already contained the expected markers (`/tmp/claude/cobalt-player-preview-navigation-diagnostic.log`, `/tmp/claude/cobalt-character-preview-boundary-4f687c9.log`, `/tmp/claude/cobalt-character-preview-timing.log`, and `/tmp/claude/cobalt-character-test-mode-boundary.log`). This proves neither a clean 8/8 suite nor the root cause of intermittent marker failures. Commit `9242d75` asserts the complete preview response marker before DOM polling; its affected browser run passed 7/7 for the six category cases plus Character (`/tmp/claude/cobalt-seven-forms-9242d75-browser.log`). Reusing Writing's unchanged pass from the earlier eight-form run yields coverage of all eight categories and 168 fields across runs, not a single clean 8/8 run. Independent static checks at `9242d75` passed: ESLint, Prettier, native Svelte checking (0 errors/0 warnings), and changed-line readability (`/tmp/claude/cobalt-9242d75-{eslint,prettier,svelte-check,readability-metrics}.log`). Existing tooling/dependency stderr remains; recurrence of the earlier browser failures is not ruled out.

- `deepwell/tests/page_form_create.rs`: native create/preview/draft through the category form, anonymous denial, invalid select rejection.
- `framerail/tests/form-editor.test.ts`: concrete draft changes, scalar codes, readonly/unknown exclusions and wire payloads.
- `framerail/tests/form-fields.test.mjs`: bounded Svelte server-rendered placeholders, text height controls, escaped adjacent help/description, radio/dropdown controls, typed selection, unknown/unselected values and static text; not browser interaction proof.
- `framerail/tests/wikitext-wizards.test.ts`: pure insertion/validation coverage for all six wizard forms, including the code-versus-other-wizard selection distinction and equation output modes.
- `framerail/tests/wikitext-wizard-component.test.mjs`: server-rendered wizard defaults/controls and escaped equation source. `aa39c6a` initializes the first equation consistently for SSR and browser. Final helper/component/action tests passed 19/19 (`/tmp/claude/cobalt-wizard-final-targeted-tests-aa39c6a.log`).
- `framerail/tests/wikitext-keyboard.test.ts`: 31 source-backed shortcut/Enter/Tab cases. Combined keyboard and existing toolbar tests passed 67/67 after `578f58e` (`/tmp/claude/cobalt-keyboard-final-578f58e-tests.log`). Actual existing/missing-editor keypresses passed 1/1 with unchanged stored source/revision and missing-page absence (`/tmp/claude/cobalt-keyboard-browser-tab-corrected.log`).
- `framerail/tests/editor-lookup.test.ts`: five action contracts, updated at `baa78f3` to call `editor_pages` with raw query text and trusted request context; short-query rejection, mapped results, and safe failures remain covered.
- `deepwell/tests/editor_lookup.rs`: native authorized current-page image lookup, image/latest-revision filtering, and anonymous/missing/forged-target rejection; 4/4 at `50c4fd7` (`/tmp/claude/cargo-editor-lookup-50c4fd7.out`).
- `deepwell/tests/editor_pages.rs`: native local endpoint coverage for case-insensitive literal prefixes, every-ASCII-space normalization, literal `%`/`_`/`\\` handling, slug order, current nondeleted titles, trusted site/user context, View filtering before the 20-result cap, and query bounds. The deterministic fixture's unique site and encoded-slug marker pass under default parallel execution, 5/5 at `024240b` (`/tmp/claude/cobalt-editor-pages-parallel-fixture-green.log`), with independent generated-artifact verification.
- `framerail/tests/image-check.test.ts`: two pure image-popup geometry tests, included in targeted 5/5 at `6dda819`; invalid-scheme behavior is covered by browser tests.
- `framerail/tests/local/image-check.mjs`: isolated Chromium popup lifecycle proof, 4/4 (load, 404, invalid scheme, close) at `/tmp/claude/cobalt-image-check-isolated-d54834f.log`.
- `framerail/tests/local/editor-wizards.mjs`: current six-wizard browser test passed 1/1 at `2650ace` (`/tmp/claude/cobalt-editor-pages-2650ace-browser-gc-stopped.log`): prefix/sort suggestions, all six inserts and Preview, empty attachment lookup, missing-target attachment exclusion, cancel focus/selection restoration, unchanged source/revision, and missing target still absent. Separate nonempty attachment/Flickr input proof remains valid from `aa39c6a`. Earlier current-runtime attempts hit Chromium `ERR_NETWORK_CHANGED`; the passing run followed user-authorized shutdown of unrelated restarting GC containers.
- `framerail/tests/local/character-form.mjs`: authenticated no-write acceptance on actual `character:atley`, passed 1/1 at `7c4ebcdf4fd9c2bef4be21e1013952c7057ce500` (`ca10516`/`7c4ebcd`; `/tmp/claude/cobalt-character-form-browser.log`). It proves the 33-field category schema—11 static, 10 text, 7 select, 5 wiki—in source order, with controls, stored values, select options, and dimensions. Existing and missing Name previews render; source, revision, and values stay unchanged and the missing target is not created. The pending static-label proof retains this as a regression case. This does not establish current hosted `after`, required-validation, or static-rendering semantics; it performs no source write or save.
- `deepwell/tests/page_draft.rs`: native draft authorization, save/restore/delete and typed-value coverage; 13 passed at `835eaf7`. `9d20256` adds the confirmed delete response assertion.
- `/tmp/claude/cobalt-page-draft-browser-fifth.log`: authenticated local browser lifecycle 1/1, including unpublished-target save/reopen/restore/publish/delete, structured typed-value preservation, and anonymous denial.
- `/tmp/claude/cobalt-preview-911748e-native.log`: native 6/6 proof of authorized missing/existing raw previews, structured preview merge and stale/invalid rejection, trusted request identity/access before submitted validation, and no revisions from invalid/conflicting preview input.
- `/tmp/claude/cobalt-page-preview-toolbar-browser.log`: browser 1/1 proof that raw create and existing previews use a real bold-toolbar caret/focus interaction and render raw heading/bold HTML; structured Name preview renders; no publication/revision/source/form changes occur; anonymous preview is denied.
- Run with Node's built-in test runner: `node --test tests/form-editor.test.ts tests/form-fields.test.mjs` from Framerail. Uses the existing `jiti` dependency for extensionless TypeScript imports and existing Svelte dependencies; no full application build/server.

Final autocomplete gates passed: Rust formatting/check after `b10e624`; frontend action5/5, Svelte0errors0warnings, scoped lint/style/format at `2e0aab1`; browser synchronization test checks at `2650ace`. Logs: `/tmp/claude/cobalt-autocomplete-{2e0aab1,b10e624}-*.log` and `/tmp/claude/cobalt-editor-pages-2650ace-*.log`.

## Known gaps (current cycle)

- [ ] Hosted-server ownership, visibility, and lifecycle parity remain unproven. The shared target/ownership model is authorized source-based inference; the captured source check has no Save/Cancel/Delete action.
- [ ] Current hosted `after` presentation is captured for Writing; watcher/checkbox behavior, required validation and server enforcement, and other static-rendering parity remain unverified. Historical source matches plain wiki textareas rather than a form-toolbar requirement. Quick-reference/snippets, raw-editor helper behavior, and ACL/history/import integration remain open.
- [ ] Confirm exact hosted-server autocomplete behavior beyond the published PHP/current-client contract. Local dedicated autocomplete is implemented and proven: case-insensitive literal slug prefix after every-ASCII-space normalization, slug order, current nondeleted titles, trusted site/user context, View filtering before a local 20-result cap, native 5/5 under default parallel fixtures at `024240b`, and browser 1/1 at `/tmp/claude/cobalt-editor-pages-2650ace-browser-gc-stopped.log`. The published PHP source is unbounded; local cap parity is not claimed.
- [x] Nonempty attachment selection and Preview: `/tmp/claude/cobalt-wizard-attachment-browser-second.log`, 1/1 at `1d06348`; original six-wizard proof remains valid and was not rerun.
- [x] Source-style image **Check** popup: isolated Chromium 4/4 and authenticated full-app 1/1 prove load, 404, invalid-scheme, close, unchanged source/revision, and attached preview. Pure bounds plus SSR/component proof passed 5/5; source lint/style/format pass. Final Svelte checking reports 0 errors and 0 Svelte warnings; both new tests pass formatting/syntax, and ESLint passes with zero warnings after `3522cc7`. Logs: `/tmp/claude/cobalt-popup-final-d54834f-*.log`, `/tmp/claude/cobalt-popup-final-3522cc7-eslint.log`. Existing missing-pnpm and Sass-deprecation stderr remains unsuppressed and is not a claim of project-wide warning-free output. The documented Playwright Fetch/`httpCredentials` popup-routing workaround is test-only and retires when transport no longer stalls the initial popup image request.
- [ ] Image size options were not captured and are not implemented. Flickr-specific support is excluded, not a pending parity task.
- [ ] Toolbar transformation behavior is source-backed and the bold path has browser proof, but full visual/editor parity is not established. Independent final verification (386) passed scoped ESLint, Stylelint, Prettier, 19/19 targeted tests and Rust formatting; prior unchanged-backend `cargo check` proof is retained. At `f6fd542`, Svelte reports 0 errors and 0 warnings, with scoped ESLint, Prettier, and Node syntax checks passing (`/tmp/claude/cobalt-form-create-types-{svelte-check,eslint,prettier,node-check}.log`). Tooling stderr remains visible and unsuppressed: missing `pnpm`, CSRF deprecation, and Sass `if()` deprecations. This is not a full-readiness claim.
- [x] Source-backed keyboard behavior: Ctrl+B/I/U, Tab caret/selection behavior, and Enter list/indent/block assistance work in both raw-editor mounts. Independent checks passed 67/67, Svelte0/0, scoped lint/style/format and cognitive complexity14 after `578f58e`; logs `/tmp/claude/cobalt-keyboard-final-578f58e-*.log` and `/tmp/claude/cobalt-keyboard-final-lint-exit.log`. This does not establish unrelated source helper or visual parity; popup and parallel-fixture proofs remain separate.
- The retained `/tmp/claude/cobalt-forms-browser-fourth.log` covers text/wiki/radio/select edits and typed/unknown-value preservation. Restored-draft merge tests and the draft browser scenario add complete-value preservation coverage; they do not establish every field type's full source parity.

## Out of scope

**Flickr integration will not be implemented** (user decision, 2026-09-23): no Flickr API/photo-info checks or Flickr-specific image resolution. The September 22 backup contains no Flickr image embeds across all 6,092 page sources; its sole Flickr reference is an ordinary external link in `writing:2025-08-23-still-falling-in`. Ordinary external links remain supported. Existing syntax-normalization code does not establish Flickr image support.

Source-site writes, production deployment without approval, source ACL parity, and unrelated visual redesign are outside this local editor implementation.
