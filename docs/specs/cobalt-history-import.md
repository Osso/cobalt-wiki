# Cobalt history import

Cobalt history import must store authorized source revision history without presenting the latest-source migration as complete history. The current migration has 6,092 pages and 1,471 files. The retained source-history inventory has 6,090 accepted source identities, 45,369 listed revisions, and 44,196 display-decoded bodies. Main-local bulk application `26bf5ef1e` produced 44,436 imported-history records across 6,022 pages: 44,187 new records and 249 preserved existing records. Offline verifier `707` passed identity, body, core-metadata, raw-HTML/acquired-field, retained Isabeau-9 capture-provenance, and authenticated 240-homepage-payload reconciliation; protected evidence is `/home/osso/.local/share/cobalt-wiki/local-full/history-bulk/independent-data-verification.json`. Final verifier `710` passed with boundaries at `/tmp/claude/cobalt-bulk-history-final-gate.json`; its manifest `/tmp/claude/cobalt-bulk-proof-manifest.json` confirms all 13 retained artifacts are present and nonempty. Static Ruff, format, and AST checks pass at `f34a32f82`; its captured fresh-process HTTP tests pass 4/4. The 1,173 anonymous source-unavailable records include the 240 already-known homepage records. The authenticated archive's 933 formerly denied bodies across 68 source pages were applied through the existing signed, filtered, missing-only importer. `application.log` exited 0 and `application-report.json` records 933 inserted records; `preservation-summary.json` records 6,116 pages, 10,092 native revisions, zero drafts, and all 44,436 existing imported-history records unchanged immediately before/after. Audit comparison of stored `source_comments` and `source_flags` against `load_page_history` records using the same `source_` field names found 933 rows with zero comment or flag mismatches. Local imported history now totals 45,369 records. Independent offline final gate `/tmp/claude/cobalt-authenticated-history-import-final-gate.json` passed. The original two-identity baseline remains unresolved: the denied numeric identity has an access-blocked history count, while the redirect destination has unknown source page ID/history. At import, all native full fingerprints matched before/after, alongside 6,116 pages, 10,092 native revisions, zero drafts, 249 existing imported-history records, and zero jobs. After UI reads, exactly two current-render cache rows changed through the existing renderer-generator upgrade: only `updated_at`, `compiled_generator`, and three compiled-HTML hash fields changed; source, title, tags, revision identity, and native count did not. That bounded exception is not history mutation and means native full-fingerprint equality must be claimed only for import, not after UI. Available-history application is verified; the parent goal remains open. Authenticated source capture was applied locally through the existing missing-only importer without intentional native/current/draft mutation. Independent offline verification at `/tmp/claude/cobalt-history-authenticated-final-verification.json` passed archive loading, raw/HTML/decoded-body hashes, display-decoded representation markers, timestamps, and 186 copied anonymous list-file pairs. The original anonymous progress hash remains `a992cb7853e650992c795a5aa63a0e506b5e9373575ca673a2241e9f04a1c7b2`; that proves this progress file only, not every original archive file. The authenticated plan records parent `/home/osso/.local/share/cobalt-wiki/local-full/import-plan.json` SHA-256 `2c7e7e2e7c5947291852e22656a61da2e0c7c1d32f10858a56f84a8ebe34fb8c`; offline parent-plan hash and checkpoint-to-stored-snapshot identity reconciliation passed at `/tmp/claude/cobalt-history-authenticated-final-verification.json`: all 933 authenticated identities were absent from the 44,436-record pre-import snapshot; this is not a live database claim. The user approved importing historical text from Wikidot's display representation even where tabs may become spaces; raw responses and that limit must remain recorded. Historical title/slug/tags may be null and 43 original full-row fingerprints cannot be reconstructed; neither limit permits a byte-exact historical representation claim. See the [current POC import contract](cobalt-poc-import.md), the [native revision import endpoint](../../deepwell/src/endpoints/import.rs), the [native import service](../../deepwell/src/services/import/service.rs), and the [stock WikiComma importer entry point](../../deepwell/importer/__main__.py).

## Original unresolved-identity status

| Original plan entry | Protected evidence | Still unresolved |
|---|---|---|
| Denied | Numeric source identity `1312457155`; revision `2` returned HTTP `200`, but parsed title and content classify it as `Private content`. | History count is access-blocked. |
| Redirect | Same-origin redirect destination `/_admin` returned HTTP `200` Site Manager; original `source/admin_manage.txt` contains the `ManageSite` module. | Source page ID and history are unknown. |

The historical plan baseline remains two unresolved identities. Neither observation implies three revisions. Independent offline verification passes with these boundaries: `/tmp/claude/cobalt-source-identities-final-gate.json`.

## Recent source identity outcome

Offline grant inspection maps the denied numeric source identity `1312457155` to authenticated source page `PrivateContent`; it does not establish its source ACL policy. The matching local target is page `3000001605` in category `100000010`. Retained local grant rows contain three anonymous virtual-role unscoped `page:view` grants and no category-scoped `page:view` grant; retained checker source plus those rows infer allow, but the checker was not executed.

The user accepted local visibility for this page only. After `6900764ce` and `09da6f49e`, underscore page names are helper pages, not form instances: the shared `template_slug` rule now uses `starts_with('_')`, consistent with rendering. Normal form parsing and malformed ordinary-form errors remain unchanged. Isolated `cobalt_test`/Redis DB 9 `page_view` RED at `/tmp/claude/cobalt-underscore-page-red.log` identified the prior field-mapping error for the `application:_public` fixture; it made no source writes. Final gate `/tmp/claude/cobalt-underscore-form-final-gate.json` passed with scope limitations: retained integration proof passed 1/1, focused helper proof passed 4/4, formatter and offline locked compile passed, and root `deploy.sh` exited 0. Protected local evidence `/home/osso/.local/share/cobalt-wiki/local-full/underscore-form-fix/` records page `3000001605` Found with `form: null` and archive-exact source, browser HTTP 200 with zero errors or POSTs, and unchanged pages `6116`, history `45369`, drafts `0`, grants `32`, and native content `10092`. Native-content hashing excludes five renderer-cache/`updated_at` fields and is not a full-row equality claim. No permission change or production deployment occurred.

The accepted page-specific visibility does not change ACLs, prove the source ACL policy, grant other pages visibility, or establish a global ACL/user-permission match. The source-denied history boundary remains: the denied identity's history count is access-blocked. The `45,369` figure remains historic acquisition/import proof, not a fresh database count. The 43 unreconstructable original full-row fingerprints remain a separate preservation-proof limit. Do not infer a blanket category deny from this evidence.

## What it must do

### Authorized source acquisition

- [ ] Acquire only authorized, available source revision records: source IDs, per-page numbers, timestamps, author IDs, comments, revision-specific metadata, raw source-module responses, and their display-decoded bodies.
- [ ] Preserve the raw response and mark each decoded historical body as display-decoded, not byte-exact. The approved tab-to-space limit does not apply to the current archived source, which remains byte-exact.
- [ ] Record unavailable or inaccessible revisions as explicit gaps; do not infer, synthesize, or silently omit them.
- [ ] Keep source creator and ACL dependencies explicitly unknown until authorized source evidence establishes them.
- [ ] Avoid an API-key dependency for acquisition or import.
- [ ] Archive each authorized page's revision lists before source bodies; persist owner-only raw and parsed responses before advancing its checkpoint, then resume without refetching verified records.
- [ ] Classify unavailable body responses and unobserved revision-number ranges as explicit gaps. Stop on unavailable or inconsistent revision-list responses; never reinterpret them as missing history.
- [ ] Bind page and site checkpoints to the requested source origin, source page identity, protected inventory, and response hashes. Use sequential one-second request spacing and at most four retry attempts for transient failures, respecting `Retry-After`.

### Imported history integrity

- [x] Preserve the current imported page and latest source while imported history is stored separately.
- [ ] Preserve source revision order, source identifiers, numbers, timestamps, author identities, comments, and revision-specific metadata when acquired.
- [ ] Never fabricate revision records, bodies, author identities, or metadata.
- [ ] Distinguish the technical migration record already present on POC pages from source history; do not represent it as a source revision.
- [ ] Reconcile every imported record and explicitly report retained gaps after import.
- [x] Preflight the complete acquired archive and identity-matched local targets before bulk writes; import only missing records, preserving matching existing history and rejecting conflicts.
- [x] Split large page histories by serialized JSON-RPC request bytes within the existing 10 MiB limit, without splitting a revision or increasing the server limit.
- [x] Bind resumable application reports to source inputs, target endpoint/site, page identities and current revision guards. After an uncertain write outcome, a fresh invocation reads persisted history before submitting remaining records; it never blindly replays matching records.

### Proof and operational safety

- [x] Acquire all 240 revision-list records (numbers 0–239) and all 240 historical source-module responses for one page through the authenticated read-only source UI.
- [x] Decode all 240 captured source-module representations with explicit display-decoded provenance; revision 239 decodes to the archived source SHA-256 `eb0478369cf1cae46acb85b994c77162e3116ce7694ca7c5399ae26e46890beb`.
- [x] Confirm that the module returns HTML-wrapped source representations rather than an independently verified byte-identical archive representation.
- [x] Store three source records idempotently in local imported-history storage without changing the current revision, compiled output, or native revision count.
- [x] Submit and reconcile the acquired 240-record homepage history set (`0`–`239`) on main local site `6000000` through `import_wikidot_history`. The protected preflight bound `home:start` page `3000000141`, source page `1310927108`, guarded current revision `3000000281`, and a zero-row starting inventory; the response reported `{"inserted":240}`. Offline independent verifier `691` passed all 28 assertions in `/tmp/claude/cobalt-main-history-independent-verification.json`: source/target payload SHA-256 values `b5cf23e403f9e34a413e3daa74f477656d6759e72ebcb4098245d806828f265c` / `96281d57f31f092df26703f8fabfac1b678ce416914199e9d2aeff8c94f24d3c`, 240 unique source IDs and numbers `0`–`239`, 240 matching bodies, raw-HTML hashes, and acquired timestamps, `100/100/40` readback, and exact 11-key metadata. The before/after fingerprints preserved 6,116 pages, 10,092 native revisions, zero drafts, and nine unrelated rows; homepage history changed `0→240`. The verifier also confirms the guarded readback script and main-UI acceptance semantics, including browser TAP `1/1`, source bodies `239/0`, pagination through 240 rows, current-render hash/reload, no Basic challenge or credentials, and no unexpected writes. This is bounded passed main-local homepage-import and main-UI proof; see the [replica status](../wiki/systems/cobalt-replica-status.md#imported-source-history).
- [x] Reconcile the current-body comparison: revision 239's decoded payload and local API body are 2,473 bytes with SHA-256 `eb0478369cf1cae46acb85b994c77162e3116ce7694ca7c5399ae26e46890beb`, matching the archived current source. The older unequal comparison artifact predates decoder correction and is retained as superseded evidence.
- [ ] Keep historical display-decoding provenance and raw responses through import. Do not promise byte-exact recovery for old bodies.
- [ ] Prevent historical bulk work from causing an outdate or rerender flood; validate queue impact before production work.
- [ ] Require review before any operation that changes native editable revisions, page identity, or source deletion policy.

## How it works

- [POC import](cobalt-poc-import.md)
- [Queue incident and recovery](../wiki/systems/cobalt-queue-recovery.md)
- [Stock WikiComma importer implementation](../../deepwell/importer/importer.py)
- [Stock WikiComma archive reader](../../deepwell/importer/site.py)
- [Native revision import endpoint](../../deepwell/src/endpoints/import.rs)
- [Native revision import service](../../deepwell/src/services/import/service.rs)
- [Current imported-history state and boundaries](../wiki/systems/cobalt-replica-status.md#imported-source-history)

## Local backend history storage

Imported source history uses `imported_page_revision`, separate from native editable revisions. Unknown historical title/slug/tags remain null; source attachment events retain their original flags. Import does not change the current revision or render pages. Read endpoints enforce current page visibility; imported entries are not native rollback targets.

- [x] Import three source records idempotently without changing current source, compiled output, or native revision count.
- [x] Read history across two cursor pages and retrieve an old body with its source author ID and representation marker.
- [x] Reject changed existing source records, duplicate revision identities, wrong-site targets, and a stale current-page revision without replacing current content.
- [x] Enforce current-page visibility using trusted request identity; JSON `user_id` cannot override it.
- [x] Preserve imported bodies during normal text pruning while removing an unreferenced text fixture.
- [x] Import and replay history for a navigation-page fixture without enqueueing rerenders.
- [x] Independently verify the integrated history backend and local pilot at `9c4ff20`: six history tests and one prune test passed; the existing isolated queue test remains valid at one passing test. Format and cargo check passed.
- [x] Run the acquired 240-revision pilot through this storage path: seven cursor pages read 240 matching bodies and metadata; idempotent replay inserted zero records. This remains isolated site `6000011` evidence, not main-local or production proof.
- [x] Submit and read back the acquired set on main local backend `2749` / site `6000000`; the protected response reported 240 inserted records. The preflight current-body identity was 2,473 bytes and SHA-256 `eb0478369cf1cae46acb85b994c77162e3116ce7694ca7c5399ae26e46890beb`, matching the protected main plan; guarded revision `3000000281`, source/target payload files, and preserved-state fingerprints remained unchanged. Offline independent verifier `691` PASS (`/tmp/claude/cobalt-main-history-independent-verification.json`) completed the bounded gate: all 28 offline assertions cover payload hashes, identifiers, every body/raw-HTML/acquired-timestamp record, preserved-state fingerprints, exact API readback-script behavior, and browser acceptance. Main UI TAP passed `1/1` on fresh `3090`: five pages read 240 metadata rows, sources were `239/0`, provenance/read-only/no-rollback behavior remained, current render hash and reload passed, and only three read POST actions occurred. This proves one main-local homepage import, not site-wide or production history; see the [replica status](../wiki/systems/cobalt-replica-status.md#imported-source-history).
- [x] Prepare an isolated local history-pilot Vite configuration at `301ae8f84`: it reuses the main Vite config exports and SvelteKit's supported flat plugin options while writing Vite cache to `.vite-history-pilot` and Kit output to `.svelte-kit-history-pilot`; both paths are ignored. Main canonical configuration remains unchanged.
- [x] Install the pilot-only user-service override at `/home/osso/.config/systemd/user/cobalt-local-framerail.service.d/10-history-pilot.conf`, selecting `vite.history-pilot.config.ts` on `5173`, and retain the GREEN browser proof at `2113c00b1`: 240 metadata rows across five pages, 239/0 body hashes, read-only/provenance/no-rollback behavior, allowlisted native and imported-history reads only, current-body hash and reload. The prior `0db5c2fc0` hydration failure and post-isolation selector correction `49f789a64` are historical; `2113c00b1` corrects an erroneous block on native history reads. Predicate extraction at `89550f5c3` is origin/path/action-equivalent, so this proof remains valid. Final verifier `684` passed ESLint with zero findings, Prettier, and Node syntax checks; applicable function metrics remain below threshold (maximum cognitive `13`, cyclomatic `15`; extracted helper `1`/`3`). Its aggregate unit-complexity `22` is not a function metric and is rejected under the skill's literal function-only threshold. This proves the bounded isolated pilot, not main/production parity, production import, or full replica history; 43 original full hashes remain unreconstructable. See the [replica status](../wiki/systems/cobalt-replica-status.md#imported-source-history).

`import_wikidot_history` accepts a guarded current revision ID and source records. `page_imported_history` lists at most 100 records, descending by source revision number; `before_revision` is exclusive. `page_imported_revision` retrieves a source body. Main and production history parity remain unestablished.

## Implementation inventory

- `deepwell/importer/site.py`: reads revision metadata and per-page `.7z` bodies keyed by revision number.
- `deepwell/importer/importer.py`: writes the stock SQLite/S3 target.
- `deepwell/importer/__main__.py`: importer command entrypoint; it does not crawl source history.
- `deepwell/src/endpoints/import.rs`: exposes `import_wikidot_page_revision`.
- `deepwell/src/services/import/service.rs`: native Wikidot revision import behavior; the endpoint expects a sequential history beginning at revision 0.
- `tools/cobalt_migration/page_history.py`: parses revision-list metadata and gaps.
- `tools/cobalt_migration/history_source.py`: decodes the source display with explicit non-byte-exact provenance.
- `tools/cobalt_migration/history_export.py`: owner-only, resumable archive for one page's list and source-body module responses; lists complete before body capture.
- `tools/cobalt_migration/history_acquire.py`: validates the protected latest-source plan and runs the page exporter sequentially, retaining explicit metadata-unresolved records.
- `tools/cobalt_migration/poc_import.py`: shared loopback JSON-RPC transport, request wire encoder, and retryable history reads.
- `tools/cobalt_migration/history_import.py`: six-argument protected-archive importer; preflights every target, sends missing records in wire-bounded requests, and resumes from persisted target state under report and current-revision guards.
- `framerail/src/routes/[slug]/[...extra]/ImportedHistory.svelte`: presents imported records separately from editable revisions, with source access and display-decoding provenance but no rollback control. Its `c7859d0` browser flow is historical; current UI proof is blocked as recorded in the [replica status](../wiki/systems/cobalt-replica-status.md#imported-source-history).

- `deepwell/src/services/import/history.rs` and `history_structs.rs`: guarded import and permission-checked reads.
- `deepwell/migrations/20260923000000_imported_page_revision.sql`: separate source-history storage.

## Tests asserting this spec

- `deepwell/tests/imported_history.rs`: native import/idempotence/current-page preservation and real two-page history reads.

`tests/cobalt_migration/test_page_history.py` has nine independently verified parser tests. `tests/cobalt_migration/test_history_source.py` has five decoder tests; all 240 protected pilot responses decode. `tests/cobalt_migration/test_history_export.py` has eight synthetic tests for resumable list/body archival, protected checkpoint validation, gaps, and bounded failures. `tests/cobalt_migration/test_history_acquire.py` covers protected-plan site orchestration. `tests/cobalt_migration/test_history_import_orchestrator.py` has actual HTTP/fresh-process tests for exact wire-bounded chunks, full per-page metadata readback, complete preflight before writes, matching-record skips, later-conflict aborts, current-revision drift guards, and post-commit lost-response resume without replay. Targeted orchestrator proof passed 4/4 at `/tmp/claude/cobalt-history-orchestrator-first-green.log`; the strengthened later-conflict RED is retained at `/tmp/claude/cobalt-history-preflight-later-conflict-red.log`. These tests do not establish source-wide acquisition or live browser transport. Independent verification at `9c4ff20` passed six history tests, one prune test, the retained isolated queue test, format, and cargo check; its prior abnormal scoped run lacked `DATABASE_URL`. Protected artifacts under `/home/osso/.local/share/cobalt-wiki/source/history-pilot/` retain raw responses, metadata, decoded bodies, calibration, and local import/readback proofs. Current API/UI evidence and deployment boundaries are maintained in the [replica status](../wiki/systems/cobalt-replica-status.md#imported-source-history). This does not establish production import, current local UI access, site-wide acquisition, or byte-exact recovery of older historical bodies. The user alone decides whether any future deployment is warranted; no deployment occurs without explicit approval.

## Known gaps (current cycle)

- [x] Verify homepage revision-list pagination and IDs: 12 pages of 20 rows cover revisions 0–239 exactly once; all 240 rows have numeric author/date fields, five source author profiles were captured, seven metadata-change diff responses were captured, and all 240 bodies were captured. This establishes only that page's listed history.
- [x] Retain the completed anonymous checkpointed acquisition inventory: 6,090 accepted source pages, 45,369 listed revisions, 44,196 decoded bodies, 1,173 `no_permission` module refusals on 69 pages, and zero unobserved revision-number ranges. Offline archive validation using `history_export._load` verifies hashes, page identities, revision lists, and decoded-body proofs for 6,087 listed pages; three supplemental pages add 115 listed and decoded records. Evidence: `/tmp/claude/history-site-2.log`, `/tmp/claude/cobalt-history-offline-archive-audit.json`, and `/tmp/claude/cobalt-history-final-archive-audit.json`. This is retained acquisition/provenance evidence, not usable imported history.
- [x] Complete authenticated acquisition for the 933 formerly denied bodies on 68 source pages. `/tmp/claude/cobalt-history-authenticated-final-verification.json` independently passes 933/933 bodies, 68/68 pages, zero refusals, archive loading, all recorded hashes, display-decoded representation markers, timestamps, and 186 copied anonymous list-file pairs. Pacing verification passes at `8ce6c99ac`: acquisition source is unchanged since `bc8d155af`, and the captured targeted suite remains 7/7. The existing signed, filtered, missing-only importer then applied the authenticated plan: `application.log` exited 0; `application-report.json` reports 933 inserted records; `api-readback.json` confirms all 933 exact bodies and core metadata; and local history totals 45,369 records. Audit comparison of stored `source_comments` and `source_flags` against `load_page_history` records using the same `source_` field names found 933 rows with zero comment or flag mismatches. Immediate preservation evidence in `preservation-summary.json` records 6,116 pages, 10,092 native revisions, zero drafts, and all 44,436 pre-existing imported-history records unchanged. Captured UI proof (`ui.log`) passes 1/1 over 178 revisions on one wiki page across four pagination batches, including newest/oldest body hashes, metadata, read-only/provenance, no unexpected POST, reload, and unchanged rendered output. This immediate-import preservation proof is distinct from later UI/cache state. Independent offline final gate PASS is recorded at `/tmp/claude/cobalt-authenticated-history-import-final-gate.json`, confirming the 933-row archive/application/provenance contract, immediate preservation, and retained 1/1 UI proof. Keep the original two-identity baseline separate: the denied numeric identity has an access-blocked history count; the redirect destination has unknown source page ID/history. Preserve the 43 original full-row fingerprint limitation, and do not claim complete source acquisition or replica history. Main-local missing-only bulk application remains verified by final verifier `710` with boundaries; the parent goal remains open.
- [ ] Resolve source-backed old title, slug, tag, and author-profile timestamp transitions.
- [x] Import the acquired 240-revision homepage pilot through separate storage and reconcile it locally: site `6000011` has 240 records, seven read cursor pages, matching bodies/metadata, and zero replay insertions; current revision/source/compiled output unchanged. The separate navigation-fixture test established zero rerender enqueueing.
- [x] Submit and reconcile the acquired homepage set on main local site `6000000`; protected preflight identifies `home:start` page `3000000141`, source page `1310927108`, guarded current revision `3000000281`, and zero existing imported rows, while the response reports 240 inserted records. Readback confirms `100/100/40` pagination; all metadata has the required 11-key set; all display-decoded bodies, raw-HTML hashes, and acquired timestamps match; current revision/body and source/target payload files remain unchanged. Preserved-state fingerprints cover 6,116 pages, 10,092 native revisions, zero drafts, and nine unrelated records; queue depth remains zero. Main browser proof passed 1/1. Offline independent verifier `691` passed the completion gate.
- [x] Independently reconcile completed missing-only main-local archive application. CLI `26bf5ef1e` processed 6,090 targets and the actual database has 44,436 imported-history records across 6,022 pages: 44,187 new plus 249 preserved. Offline verifier `707` passed all identity/body/core-metadata checks, raw-HTML/acquired fields, retained Isabeau-9 capture provenance, and the authenticated 240-homepage payload match; `/home/osso/.local/share/cobalt-wiki/local-full/history-bulk/independent-data-verification.json` is the protected root artifact. Final verifier `710` passed with boundaries at `/tmp/claude/cobalt-bulk-history-final-gate.json`; `/tmp/claude/cobalt-bulk-proof-manifest.json` confirms all 13 retained artifacts are present and nonempty. `/tmp/claude/cobalt-bulk-history-{before,after}.log` proves import-phase equality of all native full fingerprints and preserves 6,116 pages, 10,092 native revisions, zero drafts, and 249 existing imported-history rows, with zero jobs before and after. Captured current UI proof passed 2/2: 1,133 revisions across 23 pages and 223 revisions across five pages, including metadata, newest/oldest bodies, read-only/provenance, no unexpected POST, current-render hash, and reload. After UI, only selected native revisions `3000004643` and `3000007537` changed: `updated_at`, `compiled_generator`, and three compiled-HTML hash fields; `/home/osso/.local/share/cobalt-wiki/local-full/history-bulk/post-ui-native-differences.json` and native-field hashes prove every other field unchanged, including source/title/tags/revision identity and native count. This normal read-triggered cache upgrade follows the renderer-generator mismatch in `deepwell/src/services/view/service.rs:314-324` and the field-limited write in `deepwell/src/services/page_revision/service.rs:896-911`; it is not history mutation. Do not claim native full-fingerprint equality after UI. Available-history application is verified; the parent goal remains open.
- [ ] Review any future native editable-revision replacement separately; it is not required for imported source history.
- [ ] Establish source-backed creator and ACL dependencies before claiming permission parity.

## Out of scope

- Source cutover or deletion.
- API-key dependency.
- Engine change.
- Rendering work owned by the user.
- Rewriting native editable revision history, identifier rekeying, or source deletion.
