# Cobalt replica status

Verified: 2026-09-24. This records evidence, not authorization to expose source-private content or a blanket full-replica readiness claim.

## Coverage

| Capability | Proven | Remaining |
|---|---|---|
| [Local missing-only POC import](../../specs/cobalt-poc-import.md) | Immutable 6,092-page/1,471-file plan unchanged. Added 2,108 absent pages and 966 absent files with unchanged raw page/file hash readback; target totals are 6,101 active pages (plan plus nine fixtures) and 1,471 files. Original page/file rows, file revisions, and nine imported-history rows remained unchanged. Fresh inventories after injected lost page/file creation responses excluded the committed identities; search has 6,101 documents, zero pending work, and is idle; protected HTTP smoke records `/new-writing` `200` and native exact-slug search hit among 20 results; queue depth is four baseline periodic jobs; GC containers remain stopped. Existing migration `20260923000005` created the missing local `wikidot_site_change` table required by backend rendering; no site-history rows were acquired. | This completes only the local missing-page/file slice. It does not establish source ACL, author, full history/forum, rendering/visual, deployment, or blanket replica readiness. Original native revision byte identity has a known proof gap: rerendering changed `updated_at`; 18 of 61 early changed fingerprints reconstruct when normalized, but 43 original full hashes cannot be reconstructed. An exhaustive bounded artifact search found no prior full-row timestamps for those 43. Content/metadata normalized snapshots from the 4,055-page checkpoint onward match. |
| [Source theme](../../specs/cobalt-poc-theme.md) | Archived CSS/font served behind authentication; source-image URLs resolve locally and three theme images match archive hashes over public HTTPS. Browser confirms source body/title fonts, black background and 1,000px container. | Full visual/layout parity and dynamic content rendering |
| Post-import public navigation and Who article | At `70b1e73e40cbd017fa6fdd919b48f7e6bc37e574`, bounded browser proof passes 2/2 with exit 0: captured-source navigation geometry/hover with no sidebar, and Who article source hash, image bytes, and layout (`/tmp/claude/cobalt-post-import-navigation-who.log`). | Not broad visual, ACL, editor, or ListPages parity. |
| [Native runtime](../../specs/cobalt-native-runtime.md) and [gateway](../../specs/cobalt-poc-gateway.md) | Native NixOS deployment, public HTTPS, loopback services, protected runtime secrets, Basic authentication, no-index headers, successful Sakuin readiness checks | Full source page/file ACL parity; forged invalid application-session cookies still cause an error |
| [Metadata acquisition](../../specs/cobalt-page-metadata.md) | Listing completed 277/277 pages; metadata finished with 6,090 accepted records, one denied and one redirect | Complete ACL/history/creator acquisition; neither unresolved record is fabricated |
| [Forms](../../specs/cobalt-data-form-schema.md) and rendering | Whole-record schema/value handling, trusted create/edit context, native DB regressions, and authenticated local browser create and edit flows, each passed 1/1 by independent readback verification (agent 323). `d9f5f79` adds a read-only source preview with real-DB preview/form proof 6/6 and no persistence; `ffb3cc4`/`85c4e09` add frontend preview tests, and `fd02305` connects create/edit forms. Local Save Draft proof passed browser 1/1 (`/tmp/claude/cobalt-page-draft-browser-fifth.log`) and native 13 at `835eaf7`; `9d20256` proves confirmed delete response `{ deleted: true }`. It covers unpublished target save/no search hit, restore choice, publish clearing, explicit delete, typed structured restore/edit, and anonymous denial; this session updated only the local backend. Wizard browser proof passed 1/1 (`/tmp/claude/cobalt-wizards-browser-sixth.log`): all six dialogs insert and preview; page suggestions are real; the existing-page attachment lookup is empty; the missing target omits attachments; cancel restores editor focus/selection; and existing revision/source plus missing-page absence remain unchanged. Native current-page image lookup passed 4/4 at `50c4fd7` (`/tmp/claude/cargo-editor-lookup-50c4fd7.out`); helper/component proof passed 14/14 at `e574e42`, with the later first-equation selection fix `06abc0a` browser-covered; lookup actions passed 5/5 at `0a78014`. Separate nonempty attachment browser proof passed 1/1 at `1d06348`: actual `images` attachment list, loaded thumbnail/rendered Preview image, unchanged source/revision, and invalid/numeric Flickr input behavior (`/tmp/claude/cobalt-wizard-attachment-browser-second.log`). Final browser rerun after consistent SSR/browser equation initialization passed 2/2 at `aa39c6a` (`/tmp/claude/cobalt-wizards-browser-aa39c6a.log`). At `baa78f3`/`2e0aab1`, dedicated local `editor_pages` autocomplete replaced general search: case-insensitive literal slug prefix after every ASCII space becomes `-`, slug order, current nondeleted titles, trusted site/user context, and View filtering before the local 20-result cap. Native default-parallel proof passed 5/5 at `024240b` using a deterministic fixture's unique site and encoded-slug marker (`/tmp/claude/cobalt-editor-pages-parallel-fixture-green.log`), with independent generated-artifact verification; this replaces the prior shared-`Other`-text fixture deadlock and serial-only caveat. Rust formatting/check and scoped frontend gates pass. Current-runtime browser proof passed 1/1 at `2650ace` (`/tmp/claude/cobalt-editor-pages-2650ace-browser-gc-stopped.log`): prefix/sort suggestions plus all six wizard inserts and Preview; separate attachment browser proof remains retained. Earlier retries hit network-change errors during unrelated GC container restart churn; user authorized stopping all GC containers before the passing run. At `f6fd542`, Svelte reports 0 errors and 0 warnings; scoped ESLint, Prettier, and Node syntax checks pass (`/tmp/claude/cobalt-form-create-types-{svelte-check,eslint,prettier,node-check}.log`). This is not a full-readiness claim. Source-style image **Check** popup proof is complete: `6270040` implements the separate `about:blank` popup and `6dda819` supplies helper proof. Isolated Chromium passed 4/4 (load, 404, invalid scheme, close) at `/tmp/claude/cobalt-image-check-isolated-d54834f.log`; authenticated full-app proof passed 1/1 at `563d9d7` (`/tmp/claude/cobalt-image-popup-browser-auth-cache.log`) for attached preview, popup load/404/invalid-scheme/close, unchanged source/revision. Pure bounds (2) plus SSR/component (3) passed 5/5 (`/tmp/claude/cobalt-async-popup-targeted-node-6dda819.log`), proving desktop resize/move bounds but not visual comparison; scoped lint/style/format passed (`/tmp/claude/cobalt-async-popup-scoped-lint-6dda819.log`). Final new-test/type gates passed: Svelte 0 errors/0 Svelte warnings, both test files syntax/format valid, and ESLint zero warnings after `3522cc7`; see `/tmp/claude/cobalt-popup-final-d54834f-*.log` and `/tmp/claude/cobalt-popup-final-3522cc7-eslint.log`. Existing tool/dependency deprecation stderr remains documented, not suppressed. Playwright Fetch interception with `httpCredentials` stalls the initial popup image request: isolated no-interception passes, synthetic auth reproduces it, and clearing the test auth adapter after protected-gateway login restores it. This is test transport, not application behavior. Browser proof uses passive audit and persisted readback; origin auth cache retains gateway credentials, server authentication is untouched, and no global headers, userinfo URL, or fallback is used. Retire this test-only workaround when routed popup image requests no longer stall. Source keyboard support is locally verified: Ctrl+B/I/U, Tab, and Enter list/indent/block assistance in existing/missing raw editors; actual browser1/1 `/tmp/claude/cobalt-keyboard-browser-tab-corrected.log`, keyboard/toolbar67/67 and Svelte0/0 after `578f58e`, scoped lint/style/format pass. Independent draft artifact and SQL reconciliation passed; see the [editor proof](../../specs/cobalt-form-editor.md). Flickr-specific integration remains explicitly excluded by the user at `e3660f8` in the [editor scope](../../specs/cobalt-form-editor.md#out-of-scope), not remaining parity work. All archived sources parse/render without panic. [Automatic link titles](../../specs/cobalt-link-titles.md) now use actual target revisions; four affected cached pages refreshed without new revisions. [Local same-site include expansion](../../specs/cobalt-includes.md) passed six native cases: nested/empty-value body and nav expansion, unavailable/denied/foreign isolation, foreign dependency exclusion, and cycle/depth preservation. `c7859d0` integrates the user-owned rendering branch locally: ListPages, live templates, show-to handling, and FTML fixes now build locally. At `6d8361b`, one main actual-category browser batch passed 8/8 in about 59.6 seconds (`/tmp/claude/cobalt-label-visibility-6d8361b-browser.log`): Player, Chain, Arc, BGC, NPC, Application, Character, and Writing; source-defined visible labels, accessible names, defaults, Preview, reload, and existing no-write assertions passed. Writing's six `label: null` fields render no visible invented machine labels; declared field names remain `aria-label` local accessibility adaptations, not hosted assistive-parity proof. This clean batch does not erase historical form-browser failures. Static verifier 586 remains pending. | Hosted-server draft ownership/lifecycle semantics; full source rich-editor visual parity/watchers/helpers; exact hosted-server autocomplete behavior beyond the published PHP/current-client contract (local dedicated suggestions are permission-filtered slug-prefix results, capped at 20 after View checks while published PHP is unbounded); broader form/browser coverage; local browser validation of homepage/navigation/ListPages; pagination/invalidation; source include grammar; included-page/permission invalidation; inline-file domain routing; and multi-colon reference normalization |
| [Site search](../../specs/cobalt-search.md) | Backend fake-HTTP proof covers server credential use, visible compiled-text extraction, site/permission revalidation before paging, and Meilisearch document tasks. `cf1cdec` limits matching fields to `title`/`slug`/`tags`/`body`, with `site_id` filterable only; targeted search passed 8/8 after style/readability fixes `b61806e` and `6c26c0d`. Independent verifier 330 passed fmt, check, authenticated browser search 1/1, and Framerail tests 5/5. Final local inventory is 3,991 indexed documents for 3,991 live pages, zero pending updates, and an idle index (`/tmp/claude/cobalt-final-local-inventory.json`); fixture search and `401`/`noindex` checks passed. `9a9a428` ensures/drains/polls the index and `7bd7f21` validates credentials and supervises the worker. The local-full database applied `20260923000001_search_index_pending`; `20260923000002_page_listing` was also applied locally. | The three doctype fixture failures were corrected in `20a3afc` (targeted 4/4); independent bounded final evidence audit passed. Production search is enabled (see Production Meilisearch search below); ACL parity remains unproven. Existing `SearchService` bounded retries are unchanged. Rich-editor gaps remain. |
| Production rendering ([live templates](../../specs/cobalt-live-templates.md), [ListPages/CountPages](../../specs/cobalt-list-pages.md), Join, NewPage, TagCloud, PagesByTag, Rate, SiteChanges) | Deployed 2026-09-23 from `cobalt-company` with ListPages pagination, listing invalidation and view-time rerendering of pages from older builds. Text similarity to live Wikidot: atley 0.996, roster 0.928, writings 0.925, writings/p/2 0.928 (remaining: pages created after the snapshot); `stats` totals match except one post-archive RP log. NewPage renders Wikidot's form; submitting applies its `format`, page-name and existing-page checks and opens `/<page>/edit/true/title/<name>` with the title prefilled (browser test `framerail/tests/local/new-page.mjs` passes against production). `system:page-tags` matches Wikidot: all 200 cloud tags identical (hidden `_` tags excluded, glibc en_US order, sizes scaled over the shown tags) and `/tag/_completed` lists the same 5,925 pages in the same order, missing only one post-archive page. Page `created_at`/`updated_at` hold Wikidot's times for all 6,092 pages (2026-09-23), fetched by `tools/cobalt_migration/wikidot_dates.py` through Wikidot's ListPages AJAX module (25 requests); previous values are in `reconcile_20260923_page_dates`. Scores are vote sums (upstream returned a random test score); Rate shows +1 like Wikidot: the site's only vote (fakecat:testing, OzmaAsimov) is imported from WhoRatedPageModule (`tools/cobalt_migration/wikidot_votes.sql`; cast time unknown). The 46 revision authors exist as `wikidot_user` records (`tools/cobalt_migration/wikidot_users.py`, public profiles: join time, karma, account type), so they can be attributed and later claimed. Pages in data-form categories are created through the category form and saved as Wikidot's exact record ([form editor](../../specs/cobalt-form-editor.md)). SiteChanges renders Wikidot's revision list (45,376 revisions from `tools/cobalt_migration/wikidot_changes.py` in `wikidot_site_change`, 2026-09-23); page 1 matches live Wikidot item for item; its filter form works through the URL (`/p/N/perpage/N/category/C/types/NS`, Wikidot uses AJAX). New Wikidot changes arrive every 15 minutes (`cobalt-wiki-wikidot-sync.timer`: wikidot_sync.py, its SQL, then a rerender of SiteChanges pages). Changes after the archive (2026-09-22T02:10Z) are applied by `tools/cobalt_migration/wikidot_sync.py` (anonymous view source, byte-exact on the sampled pages; title/tags/dates from ListPages; writes through Deepwell as cobalt-import; prints SQL for dates and revision rows). The 2026-09-23 run created `writing:2026-09-19-a-bold-idea` and updated one page; writings now matches live apart from Wikidot's footer. Rerun with `SINCE` = newest `wikidot_site_change.changed_at`; pages whose source Wikidot denies anonymously (e.g. home:start, nav:side) are reported and skipped. The sync also carries renames, files and permanent deletions (not deployed yet; proven on the local stack 2026-09-24): `R` rows (`You successfully renamed the page: "old" to "new".`) move the replica page with page_move before the page sync; pages with `F` rows get Wikidot's complete `files/PageFilesModule` list compared with the replica (missing files downloaded from wdfiles.com and created, touched files replaced when their SHA-512 differs, files deleted only when absent and an `F` row says deleted/renamed/moved away; a removed `BairdCursive.png` was recreated byte-identical); a permanent Wikidot delete leaves no SiteChanges row, so replica pages that have `wikidot_site_change` rows but are missing from the full ListPages listing (25 requests) are deleted with page_delete only if cobalt-import created them and Wikidot answers 404 "does not exist" (more than 25 candidates: nothing deleted). Revision-list rows follow moves and are dropped for deleted pages; every outcome is in `sync-report.json`. `icon_emdee.jpg`/`icon_valentine.jpg` do not exist on Wikidot: wdfiles.com answers HTTP 200 with a 404-byte "The file does not exist" page, and the `icons` file list (468 files) matches the replica. Anonymous visibility matches Wikidot: `admin`, `application`, `membersonly` and `nav` pages return the permissions page (scoped page:view grants from `tools/cobalt_migration/private_categories.sql`, applied 2026-09-23). No page has a side bar, as on Wikidot, whose theme hides it on every page: `tools/cobalt_migration/site_navigation.sql` (applied 2026-09-24) empties the site `side_bar_page` and clears the stored side bar of 11,987 revisions. Show-to (`:snippets:suo`) regions, e.g. the top bar Admin menu, render at view time for the signed-in users they list (by slug) and are never sent to anyone else; committed on branch, not deployed; regions inside live templates or included pages stay hidden from everyone ([includes spec](../../specs/cobalt-includes.md)) | ManageSite module (admin:manage is now private like Wikidot; the module itself is not rendered), member-level category rules (Wikidot's admin settings are not visible anonymously; roles chosen: admin=admin/root, membersonly/nav=member+, application=member+ and page author), votes |

## Production rendering (2026-09-23)

`cobalt-company` is deployed through Sakuin (`cobaltWiki` input pinned in `flake.lock`; `./ops/deploy.sh`). It adds category form templates, ListPages and CountPages, show-to stripping, Wikidot comment/raw ordering, lenient image and div heads, literal `form_data`, Join markup, and fixes link updates that failed with `page_connection_pkey` when a page's many links changed.

- Data: 456 multi-colon writings were reassigned to `writing` (see [canonical names](../../specs/cobalt-canonical-names.md)); `stats` totals then match Wikidot except one RP log absent from the archive.
- Full rerender: `page_rerender` accepts `"rerender_type": "standalone"` (no dependent outdating). Before the rerender fan-out fix (below), a `full` sweep fanned out to every page through navigation outdating; NavigationOnly jobs set `updated_at` without recompiling bodies, so `updated_at` does not prove a body rerender. Sweep script: `/tmp/cobalt-standalone-sweep.py` on the droplet.
- Deploy time: Deepwell builds with crane; dependencies are cached from Cargo manifests only and the Nix build uses thin LTO. Sakuin's server builds from its Rust files only (pin commits no longer rebuild it) and also uses crane. A Deepwell code change deployed in 2m49s (previously over 10 minutes).
- Development deploys (2026-09-23, user decision while code changes daily): Deepwell and Framerail run from `/var/lib/cobalt-wiki/app` on the droplet and are updated with `install/dev-deploy.sh [deepwell|framerail|all]` — build in the flake's `deploy` shell (same glibc/libmagic store paths as the Nix packages, incremental no-LTO `deploy` profile), rsync, restart, smoke check. Production therefore runs locally built binaries, not pinned Nix packages. The units are pointed there by runtime drop-ins in `/run/systemd/system/cobalt-wiki-{deepwell,framerail}.service.d/dev.conf`, lost on reboot until the next NixOS deploy applies Sakuin's committed `appDirectory` setting (that deploy also rebuilds Deepwell's Nix dependencies once). Migrations still run from the Nix package until then. Return to Nix deploys by unsetting `appDirectory`.
- Renderer changes need no sweep: `compiled_generator` carries the Deepwell build (`DEEPWELL_BUILD_ID`, the source store hash), and the page view rerenders a page compiled by another build before serving it (one 0.1–0.4 s first view per page per deploy). A standalone sweep is still needed once to fill `page_listing` for listing pages that are never viewed.
- Includes and category `_template` pages already invalidate their dependents (12,347 include connections in production); listings invalidate through `page_listing`.
- ListPages loads the site's page metadata once per render and filters in memory. Warm full rerenders (body, navigation, links) measured 0.09–0.42 s; nested `testlist` renders in 0.38 s instead of exceeding the 500 ms preprocess budget with 52 queries.
- Local actual Atley form preview profiling at `5659f51` still exceeded the unchanged 2.5-second aggregate deadline at 2,938 ms (lexer ~1,338 ms; SQL ~229 ms). The minimal FTML whitespace-ordering change has production-grammar token identity proof only: 1/1 selected test, with 131 filtered. Its controlled lexer measurement is not an expanded end-to-end preview pass. Root deploy, actual-browser proof, and final Rust gates remain pending; no full-readiness claim.

## Access boundary

`cobalt-company.sakuin.org` is publicly reachable over HTTPS without a client tunnel. The existing Sakuin Cloudflare Tunnel connects to the loopback gateway. Every visitor still needs the shared POC login. Public tests cover page, CSS, file, download, robots and `.well-known` authentication challenges with `X-Robots-Tag: noindex, nofollow, noarchive`.

WWS does not implement complete per-page authorization. The shared gate grants its holders access to the whole POC; it must remain until source permission parity is proven and removal is authorized. Source creator identities remain unacquired, so target author/time records explicitly describe the technical migration, not original authorship.

## Local-first development workflow

Use the local preview for rendering, form, and queue work. It avoids production deployment and Nix rebuilds while code is changing.

| Component | Local endpoint/state | Management |
|---|---|---|
| Deepwell | `127.0.0.1:2748`; `cobalt_test` on PostgreSQL `25432` | `systemctl --user status|restart cobalt-local-deepwell` |
| Framerail | `127.0.0.1:5173`; hot reload | `systemctl --user status|restart cobalt-local-framerail` |
| Authenticated gateway | `127.0.0.1:3089` | `systemctl --user status|restart cobalt-local-gateway` |
| Storage | Silo `127.0.0.1:29000` | RAM-backed `cobalt-test-silo-ram`; do not restart during current file proof |
| Queue/cache | Valkey `127.0.0.1:26381`, database `1` | preview-only database; test harness uses database `0` |

Reuse `cobalt-test-postgres` and `cobalt-test-valkey`; do not start parallel copies. The disk-backed `cobalt-test-silo` was stopped after its filesystem fell below 1% free and returned HTTP 507 during the real attachment fixture. Its 50 files were copied with matching hashes to volatile `/dev/shm/cobalt-wiki-test-silo-20260922`; `cobalt-test-silo-ram` now serves ports `29000/29001`. Preserve the disk store. When disk free space exceeds Silo's reserve, stop the RAM service, copy the state back, then retire the RAM directory. Protected preview runtime files are `/home/osso/.local/share/cobalt-wiki/integration/preview.env`, `preview-deepwell.toml`, and `preview-nginx/nginx.conf`. They remain outside git and must not be printed. The local site is `6000011` (`cobalt-company`) with five archived homepage/navigation/theme-source fixtures and locally copied test-site roles.

Local observations (2026-09-22): authenticated homepage `200`, unauthenticated homepage `401`, theme CSS `200`. Browser hydration attaches the theme stylesheet and produces a black body background. The initial browser fixture showed one unresolved module and four unloaded images; attachments and form fixtures are absent. Screenshot: protected `integration/local-preview.png`. Local include expansion is committed, not deployed. Independent proof is `6/6` native cases at `df3547f` including `cd60c83`, plus FTML empty-argument `5/5`; paths are recorded below. It expands only same-site targets that pass anonymous `page:view`; foreign, viewer-private, and dynamically changed ACLs remain unsupported. Its size guard is post-expansion, not a memory cap. This is neither visual parity nor source-ACL/security proof: archived homepage/nav still require ListPages and source-grammar work. Build the debug Deepwell binary before testing backend edits; `cargo check` alone does not update the executable. The current binary is not certified to match `HEAD`. Frontend changes hot-reload through Vite. Next proof: add minimal archived attachment/form fixtures, render actual archived includes locally, then browser-edit a hydrated form, save, reload, and verify the stored value.

## Character full-form acceptance

At `7c4ebcdf4fd9c2bef4be21e1013952c7057ce500`, `framerail/tests/local/character-form.mjs` passed 1/1 against actual `character:atley` (`/tmp/claude/cobalt-character-form-browser.log`). It proves the ordered 33-field schema (11 static, 10 text, 7 select, 5 wiki), controls, stored values, options, dimensions, and existing/missing Name previews without source/revision/value mutation or creating the missing target.

Hosted Writing capture on September 24, 2026 (`/home/osso/.local/share/cobalt-wiki/source/form-editor-disposable/writing-controls-dom.json`, `writing-archive-schema.json`, `writing-form.html`, and `/tmp/claude/cobalt-hosted-writing-disposable.jpg`) confirms current presentation for lowercase hints as placeholders, the `Summary` width-80/height-3 textarea, adjacent plain `after` text, and labeled radio controls. The authorized fresh disposable editor was neither saved nor published; the direct subsequent URL was `404` with no edit form (`writing-no-publish.json`). The tab disappeared before Cancel could be observed, so lock or autosaved-draft cleanup is unproven. This does not prove server validation/enforcement, watcher or checkbox semantics, arbitrary static rich formatting, wiki toolbars, or full hosted parity; see the [form-editor SSOT](../../specs/cobalt-form-editor.md).

## Local full-preview media routing

Verified 2026-09-23 by sanitized authoritative browser evidence at `/home/osso/.local/share/cobalt-wiki/local-full/review/missing-images-correction-verification.json`. The full preview is `127.0.0.1:3090` (Framerail `5174`, Deepwell `2749`, database `cobalt_local_full`). Missing images had two local causes: the database had no attachments and the gateway omitted `ALL` file routing, so image URLs returned HTML with status `200`. The earlier comparison also used `home:start`; the public root is `home:_public` (metadata ID `1312457213`), now the local default.

The local nginx configuration mirrors the production file-route regex from `install/nixos/poc-gateway.nix`, retains no-index and trusted-site headers, and routes to WWS at `127.0.0.1:3470`. On 2026-09-24, the approved localhost-only exception removed nginx Basic Auth from this `127.0.0.1:3090` gateway and restarted its user service; application authentication remains. Production `127.0.0.1:3088` Basic Auth remains required by the [gateway contract](../../specs/cobalt-poc-gateway.md). Independent metadata proof is complete: only `127.0.0.1:3090` listened; its unauthenticated homepage and login UI returned `200`; no-auth access does not prove application permissions. Trusted headers remain, and main confirmed tracked production gateway configuration is unchanged. WWS started with its actual `S3_FILES_BUCKET` and `S3_TEXT_BLOCKS_BUCKET` contract; `.env.example`'s old `S3_BUCKET` and `ADDRESS` names are not the runtime contract. The source was unchanged; `priority-images-proof.json` established rerender proof. The correction verifier passed before that exception: authenticated `home:_public` loaded `2/2` visible images and `2/2` CSS backgrounds, with `4/4` same-origin assets returning `200`, correct MIME, decodable bytes, and matching manifest SHA-256; `home:start` loaded `19/19`, `3/3`, and `22/22` respectively. It found no failed image or asset URLs; unauthenticated access remained `401` and no-index remained present.

This is not full visual parity or blanket replica readiness. The local missing-page/file import is complete; do not start another importer or infer rendering, source-ACL, or deployment proof. See [same-site media routing](../../specs/cobalt-media-routing.md) for the rendering contract.

## Local navigation/sidebar correction

Verified 2026-09-23 at canonical revision `1310687`. The protected source baseline `/home/osso/.local/share/cobalt-wiki/local-full/review/layout-comparison-before.json`, at `1440×1000`, records a `24px` top navigation at `y=106` and no `#side-bar`; the earlier local comparison measured `182px` at `y=-52` and exposed a sidebar.

`41cf1af` nests FTML sublists in their parent list items, `eda5742` omits sidebar markup when the configured sidebar is empty while retaining enabled sidebars, `b4b25d4` persists `top_bar_page` and `side_bar_page` through `site_update`, and `1310687` scopes Sigma typography to its layout. The local full-preview setting `side_bar_page=''` reads back persisted; the two local homepages were rerendered without source or current-revision changes.

Browser proof `/tmp/claude/cobalt-navigation-browser-green.log` passed `1/1`: seven top-level tabs in one row, top-bar height `24px` at `y=106`, visible hover dropdown, and no sidebar. Independent verification remains pending. Automatic invalidation for navigation-setting changes is not implemented: only the two current local homepages were rerendered, so other cached pages can retain old navigation until refreshed.

## Local who-we-are paragraph proof

Verified 2026-09-23 at `c0d79e1`, following paragraph/comment rendering commit `2ffcebe`. The imported `who-we-are` source remains unchanged: 4,758 bytes, SHA-256 `81faa6b5a13d43bd59177409909c4c650df86550cd60601f7db36d54da260bbe`; its current revision also remains unchanged. Only that page was rerendered with local Deepwell `2749`.

The actual source document uses XHTML 1.0 Transitional. An otherwise identical standalone HTML probe measured a 5 px caption gap in XHTML Transitional and 13 px in HTML5. `c0d79e1` therefore selects the legacy doctype only for successful Wikidot content-root and slug documents; native, error, and special responses remain HTML5. Its focused response suite passed `4/4`.

At current code, `/tmp/claude/cobalt-who-we-are-browser-green.log` is green: `who-we-are` returns `200`; visible-text SHA matches source; all three images match source MIME/SHA and exact positions; at 1440×1000 the first caption is y=670, ImageBox height is 289 px, and article height is 1,605 px; the sidebar is absent; no JavaScript errors occurred. This is a bounded local browser result, not full-site visual parity/readiness. Independent browser/HTTP/data verification passed at `080849e`; evidence, scoped quality results, and the rendering contract are recorded in [paragraph formatting](../../specs/cobalt-paragraph-formatting.md). [Meilisearch site search](../../specs/cobalt-search.md) now has bounded backend and Framerail tests, and `d07869d` mounts its form in the source header; browser acceptance and end-to-end index lifecycle remain open.

## Wikidot-layout fixes, 2026-09-24

- Collapsible blocks render Wikidot's `collapsible-block` markup (folded/unfolded parts, `&nbsp;` labels, `folded` and `hideLocation`) instead of `<details>`, which showed both labels without Wikijump CSS; Framerail's `clickCollapsible` swaps the parts (`091b076`; fixture `collapsible/wikidot`; production browser test `framerail/tests/local/collapsible.mjs` 1/1).
- Tag buttons work (not deployed): `[[button set-tags +_completed -@@ text="Publish"]]` applies its changes in order (`+x` adds, `-x` removes) to the page's current tags as a tags-only Deepwell `page_edit` (Framerail action `?/setTags`, after `page_edit_permission`; Deepwell re-checks against the session), then reloads so `[[iftags -_completed]]` hides it. Visitors without edit permission get the Edit button's error popup. `-@@` removes a literal tag `@@`: an empty `%%form_raw{...}%%` in a Set Base Tags button adds it (977 archived pages carry it, all `_completed`; history shows "Added tags: @@, …" then Publish "Added tags: _completed. Removed tags: @@."). `[[button tags]]` opens the new Tags pane (page options, between Vote and History). FTML renders the buttons with `href="javascript:;"`, so they take focus and Enter; pages rendered before that keep working by click until rerendered. Current category templates use only `+_completed -@@`; the `+%%name%%`/`+%%form_raw{...}%%` and `-*` forms of older template revisions are not handled. Unit tests `framerail/tests/tag-buttons.test.ts` 8/8; browser test `framerail/tests/local/tag-buttons.mjs` 1/1 on a worktree stack against `cobalt_local_full`.
- Triple-link labels show raw-span text, so WritingBox authors read `Alli` with href `/player:alli`, identical to Wikidot (`c370654`).
- The header search box uses Wikidot's markup without a visible label, which overlapped the logo (`c911937`).
- In the Wikidot layout, blockquote lines without a space after `>` are dropped, as Wikidot's blockquote rule keeps only `>+ ` lines (`ebdf259`; unit tests in `blockquote.rs`). The site relies on it to hide text (`recruitment-status`), and `new-profile`'s `>[[div]]` / `>[[toc]]` block now renders nothing, as on Wikidot. 14 live pages have such lines, including `home:start`.
- A rerender sweep of the older pages runs after deploy `ebdf2594963b` (`/tmp/cobalt-rerender-sweep-3.log` on the host). It waits for the job queue to fall to 20 before each batch; the dependent listing rerenders it causes drain at about 230 jobs a minute. On 2026-09-24 the queue still grew from about 4,700 to 9,424 jobs while the sweep was idle: queued `full` jobs requeued their dependents (see below).

## Rerender fan-out fix, 2026-09-24

Not deployed; branch in the report. Cause (reproduced by `deepwell/tests/page_rerender_fanout.rs` on the old code): every `full` rerender, including sweep and dependent jobs, ran the outdater unconditionally and queued its includers, its whole category when it is a `_template`, and every page's navigation when it is a bar, each at depth+1. A `_template`'s category contains the template, so its job requeued the category at every depth; the inverted `rerender-skip` check runs fast loops and skips only pages last updated outside the window. A full sweep of the 14-page first fixture ran 216 jobs, up to depth 50; one nested include edit ran 35 jobs across depths 1–3. Views were not a producer: view-time rerenders are `standalone`.

Now ([rerender invalidation](../../specs/cobalt-rerender-invalidation.md)): the change queues every affected page once, including bar consumers of affected navigation pages; dependent jobs are `standalone` and queue nothing; pending jobs are deduplicated per page and type; a `full` rerender outdates only when its output changed under the same build. The final 16-page fixture: sweep 0 jobs, include edit 22 jobs (one per dependent plus one navigation rerender per page), none queued while draining.

After deploying: jobs already queued as `full` outdate only when their output changed under the current build, so the existing backlog drains without multiplying. Renderer changes need no sweep (pages rerender on view); a sweep, if run, queues nothing even as `full`.

## Accounts and header, 2026-09-24

- The header shows Wikidot's `#login-status`: "Create account or Sign in", or the user name with My account and a Sign out option (`5036436`).
- Sign in opens a Cobalt Company login page in the site theme ("Sign in to Cobalt Company", site navigation and footer) and returns to the page it came from (`5036436`, `210a527`). Wikidot's own login screen is Wikidot-branded on www.wikidot.com and is deliberately not copied.
- The account links, search box and footer sit on translucent dark panels so their text is readable over the theme background; this deviates from Wikidot by user choice (`05480b7`).
- Browser test `framerail/tests/local/login.mjs` passes on production: header link, login page with navigation and license text, sign in, back on the page with the name shown.
- My account (`/-/settings`, site theme) shows name, profile link and email and edits the session user's profile fields (real name, gender, birthday, location, website, user page, biography), email and password. Email and password changes check the current password by a Deepwell `login` whose session is logged out at once, as Deepwell has no password-check endpoint. Not offered: rename (uses name changes), avatar and locales (profile page `/-/user`). Visitors who are not signed in get a sign-in link that returns to settings. Unit tests `framerail/tests/settings.test.ts`; not yet deployed.
- The 30 Wikidot site members get native accounts from `tools/cobalt_migration/wikidot_members.py` (user decision: same data now, set-password emails at migration): the `wikidot_user` record (profile imported if the member authored no revision) is activated under the same user ID, name, slug and Wikidot account creation time, with site membership dated at the site join time and roles (`member`; `admin`/`moderator`; `root` for the master admin), a `wikidot-<id>@members.invalid` email and a random password nobody is told; existing accounts are skipped. Needs Deepwell with the `role_list`/`user_role_list`/`user_role_grant` RPCs and `joined_at` on `member_set`. Proven on local-full 2026-09-24 at `09c39fa` (30 created, 300 login guesses refused, rerun changed nothing); not yet run on production.
- Set-password links, forgotten-password emails through Mailgun and the migration-day invite tool `tools/cobalt_migration/member_invites.py` ([accounts spec](../../specs/cobalt-accounts.md)); DB and unit tests pass, not yet deployed and no email sent.
- Site members page `/-/admin/members` for site admins/root ([member admin spec](../../specs/cobalt-member-admin.md)): Wikidot's All Members / Moderators / Administrators tabs with email, role and join date, role changes, removal and email invites; Deepwell checks the session user's admin role on every call; "Site members" in the header account menu for admins only. DB tests 7/7 and Framerail tests pass; not yet deployed.

## Page routing and parser fixes, 2026-09-24

- Pages whose names have more than one colon (456 live pages, e.g. `writing:2026-09-07-ah101-homework-1:corvin-trent`) redirected to a hyphenated name and 404ed: `wikidot-normalize` 0.12 merged earlier colons into a hyphen and the view redirected to the result. Deepwell now builds against a patched copy in `deepwell/vendor/wikidot-normalize` that keeps later colons (`58ab529`). No stored slug was affected. Both example pages return 200.
- Parser fixes deployed with `297345d31155`: lenient collapsible heads with Wikidot default labels ("+ show block" / "– hide block"), include segments without `=` ignored, email token limited to real addresses (tables with `@<…>@` cells render), lenient iframe heads, and list items ending before a block close tag (`22d7d8e` and the parser-fix commits). Browser check: character:annibeth, arc:season-5, bgc:pennings, herbalists-compendium and character:mordecai show no literal `[[` and render their collapsibles, tables and iframe.
- `[[gallery]]` renders Wikidot's gallery markup from the page's image attachments (`c8028c4`); `icons` shows all 468 images in Wikidot's order.
- The 30 Wikidot site members are native accounts (`tools/cobalt_migration/wikidot_members.py`, run 2026-09-24): Wikidot account date on the user, site join date on the membership, roles member 30 / admin 5 / moderator 1 / root 1, unusable random passwords and `@members.invalid` placeholder emails until set-password emails are sent.

## Outage 2026-09-24 17:52–18:05 UTC

Deepwell failed to restart after a dev deploy: its pre-start `sqlx migrate run` used the Nix package's migrations (runtime drop-ins replace only ExecStart), and migration `20260924000000` had been applied by hand from the app directory, so sqlx rejected the database ("previously applied but is missing in the resolved migrations"). `cobalt-wiki-wws` and `cobalt-wiki-gateway` stopped as dependents. Fixed by a runtime drop-in `/run/systemd/system/cobalt-wiki-deepwell.service.d/migrate.conf` that migrates from `/var/lib/cobalt-wiki/app/deepwell/migrations`, then starting framerail, wws and gateway. The NixOS `appDirectory` deploy makes both overrides permanent. Lesson: after any manual migration, restart and check all Cobalt units, not only Deepwell.

## Production Meilisearch search

Verified 2026-09-24: production Deepwell indexes into Sakuin's existing Meilisearch (`meilisearch.service`, `127.0.0.1:7700`, v1.43.1) as index `pages`, separate from Sakuin's `anime`, `manga` and `tvshow`. It authenticates with a Meilisearch API key named `cobalt-wiki`, limited to index `pages` and to search, document, index, settings and task actions, so Cobalt cannot read or change Sakuin's indexes. `MEILISEARCH_URL` and that key (in `MEILISEARCH_MASTER_KEY`) live in the runtime-only `/var/lib/cobalt-wiki/environment`; a timestamped backup of the previous file sits beside it. Migration `20260923000001` had already queued every live page in `search_index_pending`, so restarting Deepwell backfilled the whole site. Anonymous `page_search` returned hits for `Allicat` and `writing light and darkness` and no private-category pages for `secret admin`. The backfill indexed 6,094 documents for 6,094 live pages with zero pending. Browser proof against production passed 1/1 (`framerail/tests/local/search.mjs`, fixture query `who we are` → `who-we-are`, pagination query `writing light and darkness`; `/tmp/claude/search-prod-browser.log`): 401 and `noindex` without the gateway login, header form submit, the expected hit, two distinct result pages and Previous.

## Local Meilisearch search

Verified 2026-09-23: Meilisearch `1.43.1` is active only at `127.0.0.1:27700`, with a separate private database and a server-only key. `d07869d` mounts the source header form. `c3ac4a1` transactionally enqueues search outbox work from create, edit, delete, restore, and rerender; imports use the same page-revision services. `1816423` helper proof rejects a hit whose stored body differs from current compiled HTML before visible pagination. Worker scheduling/startup and backfill are still absent. This records neither end-to-end index lifecycle, real indexed-page/database search correctness, browser acceptance, restart behavior, nor deployment; the create-browser run remains in progress with no pass claimed. Those boundaries remain in the [site search spec](../../specs/cobalt-search.md).

## Acquisition and proof artifacts

The source API remains disabled with its original settings. No API key is required or requested. Canonical names were independently enumerated; colon-to-underscore forward mapping matches archive keys without gaps or collisions. Never invert underscore substitution to infer names.

Private evidence lives under `/home/osso/.local/share/cobalt-wiki/`: immutable import plan, aggregate missing-import proofs, complete importer log, `page-db-reconciliation.jsonl`, `file-db-reconciliation.jsonl`, parser corpus results, browser capture, and sanitized local-full media verification. The aggregate proof records 2,108 added pages, 966 added files, 6,101 indexed documents, no remaining plan entries, and the normalized-revision preservation result; its rerender-drift companion records the unreconstructable 43 original full hashes. Credentials and original source content are not tracked.

The FTML failure was reproduced with both the exact archive member and a public unmatched-`))` fixture. Correcting closing-token dispatch resolved it; logging suppression and blind mutation retries were reverted. Native DB regression, Rust fmt/check, and the 6,092-source parser corpus passed. Original bytes were not rewritten.

Local include proof at `df3547f` including `cd60c83`: native `6/6` (`/tmp/claude/cobalt-includes-verify-test.log`), fmt exit `0` (`/tmp/claude/cobalt-includes-verify-fmt-latest.log`), check exit `0` (`/tmp/claude/cobalt-includes-verify-check-latest.log`), and implementation FTML empty-argument `5/5`. No deployment or homepage visual fix follows from this proof.

Attachment invalidation local proof: `b007c0b` changes first file revision invalidation from page displacement to page edit. Valid RED queued one ordinary-link-dependent rerender after attachment creation; GREEN queued zero (`/tmp/claude/cobalt-file-invalidation-green.log`). `4fa4365` adds a dedicated-empty-Redis `--ignored` fixture: it retains site-navigation fanout when the attachment owner is the navigation page. Its final `1/1` isolated proof is `/tmp/claude/cobalt-file-invalidation-isolated-green.log`; independent verification remains pending. Nothing is deployed.

`292d7ec` adds a pure duplicate-candidate planner. It preserves all non-rerender, malformed, future, received, foreign-site, or non-byte-identical records. It is not a deletion tool and does not prove semantic redundancy.

Production queue observations, September 22, 2026: at 21:32 UTC, depth `4,353,956`, Valkey used memory `1,490,998,800` bytes and RSS `1,529,503,744` bytes. A later atomic read found depth `4,350,786`, `totalsent=4,548,267`, `totalrecv=197,519`, and five head jobs that were unreceived depth-2 navigation rerenders. These are observations, not an identified producer or recovery proof. User authorized a Cobalt-preview-only maintenance window of at most 10 minutes after the attachment fix is independently verified: back up, inspect, and remove only proven-safe byte-identical pending rerender duplicates; preserve other jobs, page/file data, and rollback evidence. Maintenance has not started.

## Imported source history

Protected homepage-pilot artifacts show a local-only import into site `6000011`: 240 records inserted, with the current revision, current source, and compiled output unchanged. Readback covered 240 bodies and metadata across seven cursor pages; an idempotent replay inserted zero records. `bd9d88f` adds local UI actions, and browser proof at `c7859d0` reads all 240 imported rows across five UI pages, views revision 0 source with its protected hash, preserves display-decoding provenance, offers no imported rollback, and reloads the current page (`/tmp/claude/cobalt-history-ui-green.log`). No production history was imported. Historical bodies remain display-decoded rather than byte-exact where source rendering converted whitespace; raw source responses retain provenance, unknown historical title/slug/tags remain null, and the current archived source stays byte-exact. Revision 239's decoder, import payload, and local API body match the archived 2,473-byte SHA-256 `eb0478369cf1cae46acb85b994c77162e3116ce7694ca7c5399ae26e46890beb`; an older unequal artifact predates decoder correction. The backend uses trusted request identity (`9c4ff20`) for history visibility rather than a JSON `user_id` override.

This is partial local proof only. The final integrated history verification is not established: `/tmp/claude/cobalt-imported-history-final-scoped-tests.log` exited `1` abnormally. History UI access remains coordinated with the rendering owner; site-wide authorized acquisition, source-backed attribution/metadata transitions, production schema/API deployment, and production import remain open.

Historical note: subagents were disabled for an earlier final-work phase. Do not treat that as a current workflow restriction.
