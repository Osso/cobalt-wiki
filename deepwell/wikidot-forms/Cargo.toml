[package]
name = "wikidot-forms"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Pure, loss-aware Wikidot form schema and scalar-value compatibility"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml_ng = "0.10.0"

[dev-dependencies]
serde_json = "1"
